# Strings Functions

`strings` 当中提供了很多直接操作字符串的函数， 绝大多数都是跟 `bytes` 当中的效果一致。

可以参考 [bytes 函数介绍](/bytes/bytes.md)

这里介绍一个 `strings.EqualFold` 函数

它可以用来比较字符串是否相同， 并且可以处理大小写之间的差异

具体的案例查看[strings test](./strings_test.go)
